import styled from "styled-components";

const SearchPage = () => {
  return <Search>검색 페이지</Search>;
};

export default SearchPage;

// CSS
const Search = styled.h1`
  background-color: black;
  color: white;
  margin-left: 10px;
`;
