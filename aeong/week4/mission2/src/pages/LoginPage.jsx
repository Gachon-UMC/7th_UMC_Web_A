import styled from "styled-components";

const LoginPage = () => {
  return <Login>로그인 페이지</Login>;
};

export default LoginPage;

// CSS
const Login = styled.h1`
  background-color: black;
  color: white;
  margin-left: 10px;
`;
